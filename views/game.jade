extends layout

block content
    // BOARD
    #board
      // BOARD LEFT
      #board-left
        .avatarContainer
          p.player-name En attente d'un joueur
          .avatar-box
            p.player-health
            p.player-resource
            .screen
            img
        .button-container
          button
            | Fin du Tour
        .avatarContainer
          p.player-name= player.login
          .avatar-box
            p.player-health
            p.player-resource
            .screen
            img(src= player.avatar)
      // BOARD RIGHT
      #board-right
        .container.table
          // TOP (opponent hand)
          .top-row
            .hand
            //.menu-bar
            //  span Menu
          // MIDDLE (played cards + decks)
          .middle-row
            .cards-row
              .card-box
              .card-box
              .card-box
              .card-box
              .card-box
              .deck-container
                .deck-box#opponent-discard
                  p Defausse
                .deck-box#opponent-deck
                  p Pioche
            .cards-row
              .card-box
              .card-box
              .card-box
              .card-box
              .card-box
              .deck-container
                .deck-box#player-discard
                  p Defausse
                .deck-box#player-deck
                  p Pioche
          // BOTTOM (player hand)
          .bottom-row
            #hand
            #info-container
              h3 Nom de la carte
              p

          // end table
        // end board-right
      #endGameContainer
        div
          div
            h1
            p
            a(href="/home").button.dark-grey Revenir à l'écran principal
      // end board
    //
      End Document
      ––––––––––––––––––––––––––––––––––––––––––––––––––
    script(src="/javascripts/game.js")
    script(src="https://cdn.socket.io/socket.io-1.4.5.js")
    script.
      var player=!{JSON.stringify(player)};
      var server_url = "#{server_url}";
